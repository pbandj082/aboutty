---
"@aboutty/core": patch
"@aboutty/studio": patch
---

Fix command cursor spacing so generated SVGs keep text and cursor alignment consistent across browsers.

Refine the Studio configuration file tabs so hover states use a subtle state layer and the active tab appears connected to the code editor.
